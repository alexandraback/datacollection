#include <bits/stdc++.h>
using namespace std;

int main() {
	int T;
	cin >> T;
	for (int ii = 1; ii <= T; ii++) {
		int K, C, S;
		cin >> K >> C >> S;
		printf("Case #%d:", ii);
		for (int i = 1; i <= K; i++) {
			printf(" %d", i);
		}
		printf("\n");
	}
}