#include <bits/stdc++.h>
using namespace std;
int main(){
	int test;
	scanf("%d", &test);
	for (int w = 1; w <= test; w++) {
		int k, c, s;
		cin>>k>>c>>s;
		printf("Case #%d:", w);
		for (int i = 1; i <= s; i++) cout<<" "<<i;
		cout<<endl; 
	}
}