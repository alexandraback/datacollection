#include<iostream>
#include<string>
#include<list>
#include <fstream>
using namespace std;

int main(){
    char buf[5];
    char* s1;
    ofstream fout;
    fout.open("1.txt");
   s1 = "1.exe 0 < in.txt"        ;
        system(s1);    
        s1 = "1.exe 1 < C-small-attempt0.in"        ;
        system(s1);    
        s1 = "1.exe 2 < C-small-attempt0.in"        ;
        system(s1);    
        s1 = "1.exe 3 < C-small-attempt0.in"        ;
        system(s1);    
        s1 = "1.exe 4 < C-small-attempt0.in"        ;
        system(s1);    
        s1 = "1.exe 5 < C-small-attempt0.in"        ;
        system(s1);    
        s1 = "1.exe 6 < C-small-attempt0.in"        ;
        system(s1);    
        s1 = "1.exe 7 < C-small-attempt0.in"        ;
        system(s1);    
        s1 = "1.exe 8 < C-small-attempt0.in"        ;
        system(s1);   
        s1 = "1.exe 9 < C-small-attempt0.in"        ;
        system(s1);    
}
