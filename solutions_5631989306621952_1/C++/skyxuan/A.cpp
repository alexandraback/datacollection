#include <cstdio>
#include <algorithm>
#include <set>
#include <map>
#include <cstring>
using namespace std;
const int N = 100010;

int main() {
	int T;
	scanf("%d", &T);
	for (int cas = 1; cas <= T; ++ cas) {
		printf("Case #%d:", cas);
	}
	return 0;
}