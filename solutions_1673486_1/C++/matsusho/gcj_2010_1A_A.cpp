#include <iostream>
#include <vector>

using namespace std;

int main(){
	int i=0, n;
	cin >> n;
	while(++i<=n){
		int size;
		cin >> size;
		vector< vector<char> > table(size, vector<char>(size));
		int win_number;
		cin >> win_number;
		ALL_END:
		cout << "Case #" << i << ": " << endl;
	}
	return 0;
}