#include <cstdio>
#include <vector>

using namespace std;

int main() {
  int T;
  scanf("%d", &T);
  for (int _i = 1; _i <= T; _i++) {
    int E, R, N;
    scanf("%d%d%d", &E, &R, &N);
    vector<int> v(N);
    for (int i = 0; i < N; i++)
      scanf("%d", &(v[i]));

    printf("Case #%d: %d\n", _i, );
  }
}
