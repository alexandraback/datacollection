#include <cstdio>#include <cmath>int casei, cases;long long t, r;int main() {	scanf("%d", &cases);	for (casei = 1; casei <= cases; ++casei) {		scanf("%I64d%I64d", &r, &t);		long long x = 0;		long long num = r + r + 1;				while (num <= t) {			if (num <= 1000000LL) { //6				long long sum = (num + num + 399999996LL) * 50000000LL;				if (sum <= t) {					t -= sum;					num += 400000000LL;					x += 100000000LL;					continue;				}			}			else if (num <= 10000000000000LL) {//13				long long sum = (num + num + 39996LL) * 5000LL;				if (sum <= t) {					t -= sum;					num += 40000LL;					x += 10000LL;					continue;				}			}						t -= num;			num += 4LL;			++x;		}		/*long long a = 2;		long long b = 2 * r + 3;		long long c = 2 * r + 1 - t;		long long x = (long long)((- b + sqrt(b * b - 4 * a * c)) / 2 / a) + 1;*/		printf("Case #%d: %I64d\n", casei, x);	}		return 0;}