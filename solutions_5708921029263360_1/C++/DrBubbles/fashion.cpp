#include <iostream>
#include <string>
#include <assert.h>

// Premature optimization is the root of all evil
// This is not going to be fast enough for the large dataset.

using namespace std;

void solveacase() {
	long long B, M;
	cin >> B >> M;

	if 
	

	return ;
}

int main ( int, char** ) {

	int T;
	cin >> T;

	for ( int i = 1; i <= T; ++i ) {
		cout << "Case #" << i << ": " << endl;
		solveacase();
	}


	return 0;
}
